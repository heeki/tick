AWSTemplateFormatVersion: "2010-09-09"
Description: Tick streams
Parameters:
  name:
    Type: String
  shardCount:
    Type: Number
Resources:
  Ingest:
    Type: AWS::Kinesis::Stream
    Properties:
      Name: !Sub '${name}-ingest'
      RetentionPeriodHours: 24
      ShardCount: !Ref shardCount
  # Vwap:
  #   Type: AWS::Kinesis::Stream
  #   Properties:
  #     Name: !Sub '${name}-vwap'
  #     RetentionPeriodHours: 24
  #     ShardCount: 1
  # Analytics:
  #   Type: AWS::KinesisAnalytics::Application
  #   Properties:
  #     ApplicationName: !Sub '${name}-analytics'
  #     ApplicationDescription: 'Application for calculating VWAP'
  #     Inputs:
  #       - KinesisStreamsInput:
  #           ResourceARN !GetAtt Ingest.Arn
  #           RoleARN:
  Consumer1:
    Type: AWS::Kinesis::StreamConsumer
    Properties:
      ConsumerName: !Sub '${name}-consumer-1'
      StreamARN: !GetAtt Ingest.Arn
  Consumer2:
    Type: AWS::Kinesis::StreamConsumer
    Properties:
      ConsumerName: !Sub '${name}-consumer-2'
      StreamARN: !GetAtt Ingest.Arn
Outputs:
  outIngestPhysicalId:
    Value: !Ref Ingest
  outIngestArn:
    Value: !GetAtt Ingest.Arn
  # outVwapPhysicalId:
  #   Value: !Ref Vwap
  # outVwapArn:
  #   Value: !GetAtt Vwap.Arn
  # outAnalytics:
  #   Value: !Ref Analytics
  outConsumer1:
    Value: !Ref Consumer1
  outConsumer2:
    Value: !Ref Consumer2
